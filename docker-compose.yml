version: '3'
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: Goyglhgo
      POSTGRES_PASSWORD: lhvLHvlHvlkgLvv
      POSTGRES_DB: DklydvPvlyhvI
    volumes:
      - ./scannerHistory/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - 3200:5432

  web:
    image: dpage/pgadmin4
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: youremail@example.com
      PGADMIN_DEFAULT_PASSWORD: yourpassword
    ports:
      - 3201:80
